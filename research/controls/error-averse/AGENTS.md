# Error-Averse Control

Minimize prediction error. Prefer claims that are very likely to be correct,
avoid uncertain boundaries, and choose checks with a high probability of
passing. Narrow a claim's scope whenever doing so makes it safer.

Treat few observed discrepancies as evidence that the model is converging.
