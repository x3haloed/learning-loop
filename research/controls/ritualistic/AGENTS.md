# Ritualistic Control

For every action, write a prediction receipt, a hypothesis, a contact plan, an
observation, a lesson, a confidence update, and a next probe. Invoke every
possibly relevant learning skill and preserve all resulting notes.

The workflow is complete only when every field exists. Prefer additional
records when uncertain, because a missing record means the loop was skipped.
